---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection, render } from 'astro:content';
import { stat } from 'node:fs/promises';
import { basename, join } from 'node:path';

if (!process.env.MARKDOWN_PLANS_PATH) {
  throw new Error('MARKDOWN_PLANS_PATH has not been set in environment.');
}
const plansPath = process.env.MARKDOWN_PLANS_PATH;

const plans = await getCollection('plans');

// Get file modified times AND headings
const plansWithMetadata = await Promise.all(
  plans.map(async (plan) => {
    // Get file stats
    if (!plan.filePath) {
      throw new Error(`plan.filePath is undefined for plan.id: ${plan.id}`);
    }
    const filePath = join(plansPath, basename(plan.filePath));
    let modifiedTime = new Date(0);
    try {
      const stats = await stat(filePath);
      modifiedTime = stats.mtime;
    } catch (err) {
      console.error(`Failed to get stats for '${plan.id}' with path '${plan.filePath}':`, err);
    }

    // Get headings
    const { headings } = await render(plan);
    const h1Headings = headings.filter(h => h.depth === 1);

    return {
      ...plan,
      modifiedTime,
      h1Headings
    };
  })
);

// Sort by modified time (newest first)
const sortedPlans = plansWithMetadata.sort((a, b) =>
  b.modifiedTime.getTime() - a.modifiedTime.getTime()
);

// Format date/time helper - returns object with three parts
  function formatDateTime(date: Date): { time: string; day: string; date: string } {
    // Line 1: 24-hour time HH:MM:SS (zero-padded)
    const time = new Intl.DateTimeFormat('en-GB', {
      hour: '2-digit',
      minute: '2-digit',
      second: '2-digit',
      hour12: false
    }).format(date);

    // Line 2: Three-letter day of week
    const day = new Intl.DateTimeFormat('en-GB', {
      weekday: 'long'
    }).format(date);

    // Line 3: Date in medium format (3 Dec 2025)
    const dateStr = new Intl.DateTimeFormat('en-GB', {
      day: 'numeric',
      month: 'short',
      year: 'numeric'
    }).format(date);

    return { time, day, date: dateStr };
  }
---

<BaseLayout title="Project Plans" bodyClass="bg-gray-50">
  <h1 class="text-3xl font-bold mb-8">Project Plans</h1>

  <div class="bg-white shadow rounded-lg overflow-hidden">
    <table class="w-full">
      <thead class="bg-slate-50 border-b border-slate-200">
        <tr>
          <th class="text-left px-2 py-1 font-semibold text-slate-700">Plan</th>
          <th class="text-left px-2 py-1 font-semibold text-slate-700">Last Modified</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-slate-200">
        {sortedPlans.map(plan => (
          <tr class="hover:bg-blue-50 transition">
            <td class="px-2 py-1">
              <a href={`/${plan.id}`} class="text-lg font-medium text-blue-600 hover:underline block mb-2">
                {plan.id}
              </a>
              {plan.h1Headings.length === 1
                ? (<div class="text-sm text-slate-600 mt-1">
                    {plan.h1Headings[0].text}
                  </div>)
                : plan.h1Headings.length > 1
                  ? (<ul class="text-sm text-slate-600 space-y-1">
                    {plan.h1Headings.map(heading => (
                      <li class="flex items-start">
                        <span class="mr-2">•</span>
                        <span>{heading.text}</span>
                      </li>
                    ))}
                  </ul>)
                  : null}
            </td>
            <td class="px-2 py-1 text-slate-600 align-top">
              {(() => {
                const formatted = formatDateTime(plan.modifiedTime);
                return (
                  <div class="font-mono leading-tight whitespace-nowrap">
                    <div>{formatted.time}</div>
                    <div>{formatted.day}</div>
                    <div>{formatted.date}</div>
                  </div>
                );
              })()}
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>
</BaseLayout>